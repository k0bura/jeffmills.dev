---
import Base from "../layouts/Base.astro";
---

<Base title="Gen UI Demo — Jeff Mills" description="Two-stage LLM tool-use pipeline rendering live UI from a prompt.">
  <h1 class="text-3xl font-semibold tracking-tight">Gen UI Demo</h1>
  <p class="mt-4" style="color: var(--jm-color-muted)">
    Coming next: a Svelte island that runs the two-stage Claude tool-use
    pipeline — components, then layout — against a tiny custom Lit design
    system.
  </p>
</Base>
